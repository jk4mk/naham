import 'package:flutter/material.dart';

/// Placeholder for shared button widget.
class AppButton extends StatelessWidget {
  const AppButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
